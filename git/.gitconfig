# Managed by dotfiles (stow package: git)
# Per-machine identity lives outside this repo, in ~/.gitconfig.local
# (created by install.sh on first run) so this file stays reusable by anyone.

[include]
	path = ~/.gitconfig.local

[init]
	defaultBranch = main

[pull]
	rebase = true

[core]
	editor = vim
